(ns ceph.core-test
  (:require [clojure.test :refer :all]
            ;;[jepsen.ceph.core :refer :all]))
            [jepsen.ceph.core :as jc]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
